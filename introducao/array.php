<?php
  $lista=[
      'acucar',
      'fermento em pó',
      'ovo',
      'leite',
      'manteiga'
  ];

  // o array fica entre os colchetes para acessar precisamos colocar os valores
  // começar com o 0 
  // tem uma função que faz com ele carrega todos de um vez o print_r

  echo $lista[0];
  print_r($lista);
